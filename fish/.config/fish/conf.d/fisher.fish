# Fisher is a fish plugin manager
# Configure fisher data in its own folder so that it won't pollute the fish folder with plugins
set -U fisher_path ~/.config/fisher

source ~/.config/fisher/functions/fisher.fish